<project>
  <modelVersion>4.0.0</modelVersion>
  <groupId>net.fortytwo.ripple</groupId>
  <artifactId>ripple</artifactId>
  <version>1</version>
  <packaging>pom</packaging>
  <modules>
    <module>ripple-base</module>
    <module>ripple-rdf</module>
    <module>ripple-linkeddata</module>
    <module>ripple-core</module>
    <module>ripple-sesame</module>
    <module>ripple-neo4j</module>
    <module>ripple-demo</module>
    <module>ripple-media</module>
  </modules>

  <build>
    <directory>${basedir}/target</directory>
            <!--<plugin>
                <artifactId>maven-doap-plugin</artifactId>
                <version>1.0-beta-1</version>
                <executions>
                    <execution>
                        <phase>site</phase>
                        <goals>
                            <goal>generate</goal>
                        </goals>
                    </execution>
                </executions>
                <configuration>
                    <language>Java</language>
                    <doapFile>${basedir}/target/${project.artifactId}-${project.version}-doap.rdf</doapFile>
                    <category>xml</category>
                    <project>${project}</project>
                </configuration>
            </plugin>-->
  </build>

  <reporting>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-javadoc-plugin</artifactId>
                <version>2.0</version>
        <configuration>
          <aggregate>true</aggregate>
        </configuration>
      </plugin>
            <plugin>
                <artifactId>maven-checkstyle-plugin</artifactId>
                <version>2.1</version>
                <configuration>
                    <configLocation>${basedir}/ripple-demo/src/site/checkstyle.xml</configLocation>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>cobertura-maven-plugin</artifactId>
                <version>2.0</version>
            </plugin>
            <plugin>
                <artifactId>maven-jxr-plugin</artifactId>
            </plugin>
    </plugins>
  </reporting>
</project>
